const myText = "I am a string";
const newString = myText.replace("string", "sage");
console.log(newString);

const originals = [1, 2, 3];
const doubled = originals.map((item) => item * 2);
console.log(doubled); 


function func(a, b) {
    var sum = a + b;
    return sum;
    }
    console.log(func(1, 2));
    